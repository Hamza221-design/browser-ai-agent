version: '3.8'

services:
  web-analyzer:
    build: .
    container_name: web-analyzer
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - config.env
    volumes:
      - ./logs:/app/logs
      - ./db:/app/db
      - ./db/chroma_models:/app/db/chroma_models
    restart: unless-stopped